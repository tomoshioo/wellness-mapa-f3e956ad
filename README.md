# Wellness mapa

Statická stránka (Leaflet + OSM). Generuje se skriptem `build_map.py` ve vaultu (projekt Sportcentrum-P17, složka `cenotvorba-wellness-2026-09/tools/`); sem se kopíruje jen výsledné `index.html`.
